exports.views = {
    tasks: {
        map: function (doc) {
            emit(doc.name, null);
        }
    }
};

exports.blogposts_by_created = {
    tasks: {
        map: function (doc) {
            emit(doc.name, null);
        }
    }
};